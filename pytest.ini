[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
norecursedirs = tests/analysis/_archive_

# Register custom markers to avoid warnings
markers =
    order: Custom marker for test ordering (used by pytest-order plugin if installed)

